# Local source sheets

Place the two original, unmodified 1536×1024 source sheets here as
`locations.png` and `location-atlas.png` before running the extractor.

The masters are intentionally not tracked because this repository's patch/PR
transport accepts text files only. The committed game-ready assets are textual
SVG containers with embedded PNG data, so they remain self-contained without
binary Git objects.
