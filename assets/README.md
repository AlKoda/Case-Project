# Assets

Add the final `.webp` files to the matching directories:

- `characters/`: suspect and witness artwork
- `locations/`: evidence room, office, and parking lot artwork
- `evidence/`: keycard, security log, and photograph artwork
- `ui/`: case folder, pin, and stamp artwork

The `.gitkeep` files preserve these directories until their image assets are added.
